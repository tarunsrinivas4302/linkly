{
  "name": "url-shortener",
  "version": "1.0.0",
  "main": "server/index.js",
  "scripts": {
    "start": "npm install && cd client && npm install && npm run build-client && npm run server",
    "build-client": "cd client && npm run build",
    "server": "node server/index.js",
    "client": "vite --config client/vite.config.js",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "npm run build:client && npm run build:server",
    "build:client": "cd client && vite build",
    "build:server": "echo \"Server build step (if any)\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "concurrently": "^8.2.2",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.5.4",
    "nodemon": "^3.1.4",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "npm-watch": "^0.13.0"
  }
}
